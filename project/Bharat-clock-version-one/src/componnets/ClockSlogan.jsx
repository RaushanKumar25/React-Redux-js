const ClockSlogan = () => {
  return (
    <p className = "lead">
      More than timekeeping. A reminder that every second is a chance to build a better India.</p>
  );
};

export default ClockSlogan;
